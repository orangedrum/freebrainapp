# Copy to .env (never commit .env — it may hold secrets).
# The VAPID *public* key ships in the JS bundle by design (push services need it);
# the *private* key lives ONLY in Supabase → Project Settings → Secrets as VAPID_PRIVATE_KEY.
VITE_VAPID_PUBLIC_KEY=REPLACE_WITH_VAPID_PUBLIC_KEY
